# 构建产物与本地数据（不入库）
node_modules/
build/
dist/
*.blob
*.exe

# 注册后端的账号库：里面是真实的密码哈希，绝不入库
server/data/
server/data/*.json
*.tmp

# 测试与截图的可再生产物
_check-out.txt

# 与本项目无关的工作区文件（属于其他任务）
_pdftext/
_toolcheck.txt
_frame.html

# 编辑器与系统
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# 独立仓库，不嵌进父仓库
att-tools/

# Hugging Face ���ƣ�������⣩
tools/music-ai/.hf-token
.hf-token

# վ�ںϳ���Ŀ�ĵ�����������һ�������������ɣ�����⣩
assets/audio/*-export.wav

# CDP �����õ����������Ŀ¼����ǧ���ļ�����������⣩
preview/_cdp-profile/

# 浏览器配置目录一律不入库（脚本已改用系统临时目录，这里是兜底）
preview/_*-profile/

# 第三方角色立绘与语音：著作权归鹰角网络／蒙塔山所有，仅在本地演示中使用，绝不入库。
# 换角色时把新文件名加在这里，规则一致：从网上取的素材都不进仓库。
# 素材不在仓库里，所以线上靠 release 附件（Gitee 一份、GitHub 一份），两处都取不到才降级。
# 语音是她的官方日配，取自 PRTS 的语音记录（torappu.prts.wiki/assets/audio/voice/char_249_mlyss）。
assets/img/muelsyse.png
assets/img/muelsyse-bust.png
assets/img/muelsyse-chibi.png
assets/audio/muelsyse-jp-*.mp3
